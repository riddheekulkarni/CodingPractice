#include <stdio.h>

int main()
{
    int a,i,fact=1;
    printf("enter value of a:");
    scanf("%d",&a);
    for(i=1;i<=a;i++)
    {
        fact=fact*i;
    }
    printf("factorial of %d = %d",a,fact);
}