#include <stdio.h>

int main()
{
    int year;
    printf("enter a year:");
    scanf("%d",&year);
    if(year%4==0)
    {
        printf("the entered year is leap");
    }
    else
    {
        printf("the entered year is not leap");
    }
}